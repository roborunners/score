<android.support.v7.widget.CardView
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:layout_margin="6dp"
    android:clickable="true"
    android:foreground="?android:attr/selectableItemBackground"
    app:cardElevation="@dimen/elevation_card"
    app:cardCornerRadius="2dp"
    app:cardUseCompatPadding="true"
    app:contentPaddingTop="@dimen/margin_activity_vertical"
    app:contentPaddingBottom="@dimen/margin_activity_vertical"
    app:contentPaddingLeft="@dimen/margin_activity_horizontal"
    app:contentPaddingRight="@dimen/margin_activity_horizontal">

    <LinearLayout
        android:id="@+id/container"
        android:orientation="horizontal"
        android:layout_width="match_parent"
        android:layout_height="match_parent">

        <TextView
            android:id="@+id/label"
            android:layout_width="0dp"
            android:layout_height="wrap_content"
            android:layout_weight="1"
            android:layout_gravity="center_vertical|start"
            android:textSize="@dimen/text_scoreelement" />

        <EditText
            android:id="@+id/number"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_gravity="center_vertical"
            android:inputType="number"
            android:hint="@string/number_zero" />
    </LinearLayout>
</android.support.v7.widget.CardView>